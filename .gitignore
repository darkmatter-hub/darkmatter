node_modules/
.env
*.pem
*.key
.DS_Store
*.witness.private.pem
backups/

# Agent worktrees and stray local artifacts (swept in accidentally 2026-08-19)
.claude/
instagram_post_*/
0

# Internal, never published. Strategy, competitive analysis, security detail
# and the park record. These were briefly committed to a public repo; see
# private/PARK.md for what that means and what is still in git history.
private/

# Python bytecode from running the verifier and its tests
__pycache__/
*.pyc
